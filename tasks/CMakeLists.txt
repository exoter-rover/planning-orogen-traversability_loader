# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(traversability_loaderTaskLib)
ADD_LIBRARY(${TRAVERSABILITY_LOADER_TASKLIB_NAME} SHARED 
    ${TRAVERSABILITY_LOADER_TASKLIB_SOURCES})
add_dependencies(${TRAVERSABILITY_LOADER_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${TRAVERSABILITY_LOADER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${TRAVERSABILITY_LOADER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${TRAVERSABILITY_LOADER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${TRAVERSABILITY_LOADER_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${TRAVERSABILITY_LOADER_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${TRAVERSABILITY_LOADER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${TRAVERSABILITY_LOADER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${TRAVERSABILITY_LOADER_TASKLIB_HEADERS}
    DESTINATION include/orocos/traversability_loader)

